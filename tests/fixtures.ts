/**
 * Shared test fixtures.
 *
 * Constants used across multiple test files to reduce duplication
 * and provide a single source of truth for test data.
 */

/** A known blog post slug used for representative blog post tests. */
export const SAMPLE_BLOG_SLUG = '250mm-trading-card-box';
