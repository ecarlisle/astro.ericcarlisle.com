import SocialLinksInFooterFixture from './SocialLinksInFooterFixture.astro';

const meta = {
  title: 'Components/SocialLinks/In Footer',
  component: SocialLinksInFooterFixture,
  parameters: {
    docs: {
      description: {
        component:
          'SocialLinks rendered within a footer context. The footer uses production spacing tokens and borders.',
      },
    },
  },
};

export default meta;

export const InFooter = {};
